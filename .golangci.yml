linters:
  enable:
    - gofmt
    - golint
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - typecheck
    - gocritic
    - gosimple
    - unused
    - bodyclose
    - dupl
    - misspell

linters-settings:
  govet:
    check-shadowing: true
  dupl:
    threshold: 100
  gocritic:
    enabled-tags:
      - performance
      - style
      - experimental
    disabled-checks:
      - commentedOutCode

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - dupl
        - errcheck